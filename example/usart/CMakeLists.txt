include(../build_binary.cmake)


if (${BUILD_EXAMPLE} STREQUAL "ALL")
  add_binary(rx_dma_var_len)
  add_binary(rx_dma)
else()
  if(${usart_files} STREQUAL "")
    add_binary(rx_dma_var_len)
    add_binary(rx_dma)
  else()
    foreach(src ${usart_files})
      add_binary(${src})
    endforeach()
  endif()
endif()
