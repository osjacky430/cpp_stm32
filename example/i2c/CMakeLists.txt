include(../build_binary.cmake)

if (${BUILD_EXAMPLE} STREQUAL "ALL")
  add_binary(i2c_imu)
  add_binary(i2c_driver_imu)
else()
  if(NOT DEFINED ${i2c_files})
    add_binary(i2c_imu)
    add_binary(i2c_driver_imu)
  else()
    foreach(src ${i2c_files})
      add_binary(${src})
    endforeach()
  endif()
endif()
