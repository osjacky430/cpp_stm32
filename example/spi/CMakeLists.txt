include(../build_binary.cmake)

if (${BUILD_EXAMPLE} STREQUAL "ALL")
  add_binary(spi_imu)
  add_binary(spi_driver_imu)
else()
  if(NOT DEFINED ${spi_files})
    add_binary(spi_imu)
    add_binary(spi_driver_imu)
  else()
    foreach(src ${spi_files})
      add_binary(${src})
    endforeach()
  endif()
endif()
